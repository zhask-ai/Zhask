apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-dashboard-monitor
  labels:
    app: integrishield-dashboard
spec:
  selector:
    matchLabels:
      app: integrishield-dashboard
  endpoints:
    - port: http
      path: /api/health
      interval: 15s
