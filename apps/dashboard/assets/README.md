# Dashboard assets

Place the Claude SAP Security Assistant avatar here:

- `ai-assistant.png` — cat-with-paw-up "Ai" badge image. Used by `.chat-fab` and `.chat-header-icon` in `styles.css`.

Recommended: square PNG, 256×256 or larger, transparent or dark background.
